[pytest]
# This tells pytest to add 'src' to the sys.path automatically
pythonpath = src
testpaths = tests
# This registers the async mark to fix your warnings
markers =
    asyncio: mark test as an asyncio test